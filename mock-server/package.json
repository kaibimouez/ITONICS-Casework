{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock API using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "0.17.3"
  }
}
